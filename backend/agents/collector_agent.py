# Collector Agent Stub
def run_collector(target):
    """
    Collect images from social media based on the target (hashtag, username, keyword).
    Uses SNScrape, Instaloader, or yt-dlp in full implementation.
    """
    print(f"Collecting data for target: {target}")
    return {"status": "success", "images": []}
